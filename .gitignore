# OS / Editor artifacts
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Node (if you ever add a dev server via npm)
node_modules/
package-lock.json

# Deployment artifacts
.vercel/
.netlify/

# Environment variables — never commit secrets
.env
.env.local
.env.production

# Editor configs (optional: keep if you want to share settings)
.vscode/settings.json
